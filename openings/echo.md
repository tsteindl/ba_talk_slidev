# A distance we cannot see

<TalkBody :number="1" module="echo" :index="0" :clicks="$clicks" :page="$page" />

<!--
Hidden reflector at unknown distance. A pulse can reach it although direct sight or access is unavailable. Homogeneous propagation and known sound speed c are assumed. [Sources] elementary response model or geometry stated on slide; openings.js. Schematic, not measured calibration data.
-->

---
clicks: 1
---

# Send a pulse. Time the echo.

<TalkBody :number="1" module="echo" :index="1" :clicks="$clicks" :page="$page" />

<!--
Deterministic prerecorded outgoing and return paths illustrate a round trip; animations represent process, not calibrated propagation speed. Measure delay Δt of the reflection from the intended target. [Sources] elementary response model or geometry stated on slide; openings.js. Schematic, not measured calibration data.
-->

---

# Echo delay → infer distance

<TalkBody :number="1" module="echo" :index="2" :clicks="$clicks" :page="$page" />

<!--
Round-trip distance is 2d, so Δt=2d/c and d_hat=c times estimated delay /2. Known propagation speed, target association, and negligible extra latency are assumptions; no arbitrary numerical speed is needed. Next quantum metrology slide uses the same layout, but single outcomes are probabilistic and we first estimate a probability. [Sources] elementary response model or geometry stated on slide; openings.js. Schematic, not measured calibration data.
-->
